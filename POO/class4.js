var name = 'Jhan', age = 19;

function printAge(n, a) {
  console.log(`${n} has ${a} years old`);
}

printAge(prompt("Type your name"), prompt('Type your age'));


// printAge('Andrea', 20);
// printAge('Eshta', 25);

// //JavaScript don't fail if the expected values are different. 

// printAge(25, 'eshta');
